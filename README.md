📊 تحلیل نسبت سکه به طلا (TGJU Gold–Coin Ratio Analyzer)

این پروژه برای جمع‌آوری، پردازش و تحلیل داده‌های قیمت طلا و سکه از وب‌سایت TGJU طراحی شده است.
هدف اصلی پروژه، محاسبه نسبت میانگین قیمت سکه به طلا در بازه‌های زمانی مختلف و نمایش آن به‌صورت نمودار می‌باشد.

🚀 روند کلی اجرای پروژه

پروژه شامل ۳ مرحله اصلی است:

1️⃣ دریافت داده‌ها از TGJU (get_json.py)

در این مرحله:

اسکریپت get_json.py اجرا می‌شود

کاربر:

دو تاریخ (شروع و پایان)

دو API مربوط به طلا و سکه
را وارد می‌کند

اسکریپت به‌صورت خودکار داده‌ها را از سایت TGJU دریافت می‌کند

📥 برای هر روز، اطلاعات زیر ذخیره می‌شود:

قیمت باز شدن روز

کمترین قیمت روز

بیشترین قیمت روز

قیمت بسته شدن روز

تاریخ میلادی

تاریخ شمسی

📄 خروجی این مرحله:

data.json


که شامل داده‌ی خام طلا و سکه به‌صورت روزانه است.

2️⃣ تبدیل داده خام به نسبت سکه به طلا (convert_json_to_ratio.py)

در این مرحله:

فایل convert_json_to_ratio.py اجرا می‌شود

داده‌های خام موجود در data.json پردازش می‌شوند

🔢 برای هر روز:

میانگین قیمت طلا محاسبه می‌شود:

(بیشترین قیمت + کمترین قیمت) / 2


میانگین قیمت سکه نیز به همین روش محاسبه می‌شود

نسبت سکه به طلا محاسبه می‌گردد:

ratio = sekee_avg / tala_avg


📄 خروجی این مرحله:

{
    "date_gregorian": "2025/12/27",
    "date_shamsi": "1404/10/06",
    "tala_avg": 148514500.0,
    "rob_zir": 214000000.0,
    "ratio": 1.440936743550293
}


این خروجی برای تمام روزهای موجود در بازه زمانی تولید می‌شود و در فایل output.json ذخیره می‌گردد.

3️⃣ ترسیم نمودار نسبت (chart_generator.py)

در مرحله نهایی:

اسکریپت chart_generator.py اجرا می‌شود

داده‌های output.json خوانده می‌شوند

نمودار تغییرات نسبت سکه به طلا در طول زمان رسم می‌شود

📈 این نمودار ابزار مناسبی برای:

تحلیل رفتار بازار

بررسی حباب سکه نسبت به طلا

تصمیم‌گیری در معاملات کوتاه‌مدت و میان‌مدت

🛠 تکنولوژی‌ها و ابزارها

Python

Requests

JSON

Matplotlib

تحلیل داده‌های بازار مالی

📌 کاربردهای پروژه

تحلیل حباب سکه

مقایسه رفتار قیمت طلا و سکه

استفاده در سیستم‌های معامله‌گری و ربات‌های تحلیل بازار

داده‌کاوی بازار فلزات گران‌بها

▶️ نحوه اجرا
python get_json.py
python convert_json_to_ratio.py
python chart_generator.py

📎 نکته مهم

ممکن است در برخی روزها داده‌ی سکه یا طلا موجود نباشد.
در این پروژه، برای جلوگیری از خطا، محاسبات بر اساس حداقل داده‌ی مشترک انجام می‌شود.
